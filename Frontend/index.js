import { registerRootComponent } from 'expo';  // Importing the function to register the root component

import App from './App';  // Importing the main app component (App)

registerRootComponent(App);  // Registering the App component as the root of the app
